import React from "react";

function Footer() {
  return (
    <div
      style={{
        height: "40px",
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        justifyContent: "center",
        fontSize: "1rem",
      }}
    >
      <h3> MovieDB</h3>
    </div>
  );
}

export default Footer;
